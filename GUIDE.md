# 📚 Полное Руководство File Exchanger

## Содержание
1. [Что это?](#что-это)
2. [Системные требования](#системные-требования)
3. [Установка](#установка)
4. [Запуск сервера](#запуск-сервера)
5. [Подключение клиента](#подключение-клиента)
6. [Использование](#использование)
7. [Решение проблем](#решение-проблем)
8. [Продвинутые настройки](#продвинутые-настройки)

---

## Что это?

File Exchanger - это простая и красивая система для обмена файлами между компьютерами в локальной сети.

**Особенности:**
- 🚀 Быстрая и лёгкая
- 🎨 Красивый современный интерфейс
- 📱 Работает в браузере (без установок)
- 🔒 Безопасно для локальной сети
- 📦 Поддерживает любые файлы и размеры

---

## Системные требования

### Для сервера (компьютер-хост):
- **OS:** Windows 7+, macOS 10.9+, Linux
- **Python:** 3.6 или выше
- **RAM:** 64 MB минимум
- **Диск:** 100 MB свободного места
- **Сеть:** Локальная LAN или WiFi

### Для клиента (другой компьютер):
- **OS:** Windows, macOS, Linux, мобильный
- **Браузер:** Chrome, Firefox, Safari, Edge (любой современный)
- **Интернет:** НЕ требуется (локальная сеть)
- **Установка:** Не требуется

---

## Установка

### Проверка Python

1. **Откройте PowerShell** (поиск → PowerShell)
2. **Введите:**
```powershell
python --version
```

3. **Если показывает версию (например, "Python 3.9.0")** - всё хорошо
4. **Если ошибка:**
   - Скачайте Python: https://www.python.org/downloads/
   - При установке обязательно поставьте галку "Add Python to PATH"
   - Перезагрузитесь

### Проверка, что всё готово

```powershell
# Перейдите в папку
cd C:\Users\<ваше_имя>\Desktop\file-exchanger

# Проверьте что там есть
ls
```

Должны быть видны файлы:
- `server/` (папка)
- `start_server.bat`
- `README.md`

---

## Запуск сервера

### Способ 1: Простой (рекомендуется)

1. **Найдите файл** `start_server.bat` в папке `file-exchanger`
2. **Дважды кликните** на него
3. **Должно открыться окно** с текстом типа:
```
==================================================
  📁 FILE EXCHANGER SERVER
==================================================
✓ Server started successfully!

Access from other computers:
  → http://192.168.X.X:8888
```

4. **Запомните IP адрес** (в примере: `192.168.1.100`)

### Способ 2: Через PowerShell

1. **Откройте PowerShell**
2. **Введите:**
```powershell
cd C:\Users\<ваше_имя>\Desktop\file-exchanger\server
python server.py
```

### Способ 3: Узнать IP отдельно

Если нужно узнать IP адрес:

1. **Найдите и дважды кликните** `get_ip.bat`
2. **Или введите в PowerShell:**
```powershell
ipconfig
```

Ищите строку "IPv4 Address:" (обычно выглядит как `192.168.x.x`)

---

## Подключение клиента

### На другом компьютере в той же сети

1. **Откройте браузер** (Chrome, Firefox, Safari и т.д.)
2. **В адресную строку введите:**
```
http://192.168.1.100:8888
```
Вместо `192.168.1.100` подставьте IP адрес сервера

3. **Нажмите Enter**
4. **Готово!** Должен загрузиться красивый интерфейс

### Если на одном компьютере с сервером

В браузере введите:
```
http://localhost:8888
```

---

## Использование

### Загрузка файла

**Способ 1: Перетаскивание**
1. Выделите файл в проводнике
2. Перетащите его на область "Загрузить файл"
3. Готово!

**Способ 2: Нажатие**
1. Нажмите на область "Загрузить файл"
2. Выберите файл в проводнике
3. Нажмите "Открыть"

### Скачивание файла

1. Найдите файл в списке "Доступные файлы"
2. Нажмите кнопку "📥 Скачать"
3. Файл загрузится в папку "Загрузки"

### Удаление файла

1. Найдите файл в списке
2. Нажмите кнопку "🗑️ Удалить"
3. Подтвердите удаление

### Обновление списка файлов

Список обновляется автоматически каждые 5 секунд, но вы можете:
- Обновить страницу: F5
- Обновить кэш: Ctrl+F5 (Windows/Linux) или Cmd+Shift+R (Mac)

---

## Решение проблем

### "Не удаётся подключиться"

**Проблема:** Получаю ошибку "Не удаётся установить соединение"

**Решение:**
1. Убедитесь, что сервер запущен (проверьте окно PowerShell)
2. Проверьте IP адрес (запустите `get_ip.bat`)
3. Оба компьютера должны быть в ОДНОЙ сети (одна WiFi/LAN)
4. Попробуйте использовать `localhost:8888` если тестируете на одном ПК

### Брандмауэр блокирует

**Проблема:** Сервер запущен, но не подключаюсь

**Решение на Windows:**
1. Откройте Параметры
2. Перейдите: Конфиденциальность → Брандмауэр Windows
3. Нажмите "Разрешить приложению через брандмауэр"
4. Найдите Python или добавьте порт 8888
5. Включите для приватных сетей

**Решение на Mac:**
1. System Preferences → Security & Privacy → Firewall
2. Нажмите "Firewall Options"
3. Добавьте Python в исключения

### Порт уже занят

**Проблема:** Ошибка "Address already in use"

**Решение:**
1. Запустите `network_test.bat`
2. Убейте процесс что занимает порт:
```powershell
netstat -ano | find ":8888"
# Скопируйте PID
taskkill /PID <PID> /F
```

3. Или измените порт в `server.py` (строка `PORT = 8888`)

### Медленная загрузка/скачивание

**Проблема:** Файлы загружаются/скачиваются медленно

**Это НЕ проблема приложения!** Это проблема сети:
- Проверьте скорость WiFi
- Используйте проводное подключение (LAN)
- Закройте другие приложения что используют сеть

### Файл не появляется в списке

**Проблема:** Загрузил файл, но его нет в списке

**Решение:**
1. Обновите страницу (F5)
2. Подождите 5 секунд (автоматическое обновление)
3. Проверьте брандмауэр
4. Посмотрите папку `server/shared_files/`

### Не могу удалить файл

**Проблема:** Кнопка удаления не работает

**Решение:**
1. Убедитесь, что файл не открыт в другой программе
2. Проверьте права доступа
3. Перезагрузитесь

---

## Продвинутые настройки

### Изменить порт

1. Откройте файл `server/server.py` в текстовом редакторе
2. Найдите строку: `PORT = 8888`
3. Измените на нужный порт, например: `PORT = 9999`
4. Сохраните файл
5. Перезапустите сервер

### Изменить папку хранения файлов

1. Откройте `server/server.py`
2. Найдите: `UPLOAD_FOLDER = Path("shared_files")`
3. Измените путь, например: `UPLOAD_FOLDER = Path("C:/MySharedFiles")`
4. Сохраните и перезапустите

### Пользовательский интерфейс

Интерфейс находится в файле `server/index.html`

Вы можете:
- Изменить цвета (найдите `#667eea` и `#764ba2`)
- Изменить текст
- Добавить новые функции (требуется знание HTML/CSS/JS)

### Запуск на другом порту через PowerShell

```powershell
cd C:\Users\<ваше_имя>\Desktop\file-exchanger\server

# Установите переменную окружения
$env:PORT = 9999

# Запустите сервер
python server.py
```

### Автозапуск при старте Windows

1. Создайте ярлык на `start_server.bat`
2. Скопируйте его в `C:\ProgramData\Microsoft\Windows\Start Menu\Programs\Startup`
3. При следующей загрузке сервер запустится автоматически

---

## Часто задаваемые вопросы

### Нужен ли интернет?
**Нет!** File Exchanger работает в локальной сети без интернета.

### Можно ли использовать через интернет?
Технически можно через VPN, но:
- Требуется VPN настройка
- Рекомендуется добавить аутентификацию
- Используйте HTTPS для безопасности

### Какой максимальный размер файла?
Ограничен только памятью сервера. Протестировано на 1GB+ файлах.

### Сколько людей может подключиться?
Неограничено! Сервер может обрабатывать несколько одновременных клиентов.

### Где хранятся загруженные файлы?
В папке `server/shared_files/` рядом с `server.py`

### Можно ли закрыть сервер когда кто-то качает?
Не рекомендуется. Лучше дождаться завершения или предупредить пользователей.

### Как резервировать файлы?
Скопируйте папку `server/shared_files/` в другое место.

---

## Технические детали

### Архитектура
```
Client (Browser) ←HTTP→ Server (Python)
                ↓
           Database (Filesystem)
```

### API endpoints
- `GET /api/files` - список файлов
- `POST /api/upload` - загрузить файл
- `GET /api/download/<filename>` - скачать файл
- `DELETE /api/delete/<filename>` - удалить файл
- `GET /api/server-info` - информация сервера

### Производительность
- Одновременные загрузки: неограничено
- Скорость: зависит от сети
- Память: O(n) где n - размер файла

---

## Безопасность

⚠️ **Локальная сеть:** Безопасно использовать
⚠️ **Через интернет:** Требуется VPN + HTTPS
⚠️ **Доступ:** Любой может управлять файлами (нет пароля)

**Рекомендации:**
- Используйте только в локальной сети
- Если нужна безопасность - добавьте аутентификацию
- Не открывайте порт в роутере без необходимости

---

## Поддержка и вклад

Если вы нашли ошибку:
1. Проверьте раздел "Решение проблем"
2. Посмотрите логи в PowerShell окне
3. Попробуйте перезапустить сервер

---

## Лицензия

MIT License - используйте свободно!

---

**Спасибо за использование File Exchanger! 🙏**

Версия: 1.0
Дата: 2025
